<p id="notice"><%= notice %></p>
<div> <%= @course[0] %> </div>

<h3>COURSE INFO </h3>

<dir> PUT SOME INFORMATION HERE about the class</dir>

<p id="notice"><%= notice %></p>
<br>
<h3>Groups INFO </h3>

<h4>Groups you are currecntly a part of <%= @mygroups.length %>!</h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

    <% if @mygroups.length == 0 %>
    	<tr> TABLE IS EMPTY </tr>
    <% end %>
  <tbody>
    <% @mygroups.each do |group| %>
      <tr>
      <td><%= group[1]["name"] %> </td>
        <td><%= link_to 'Show Details', course_path(group[0]) %></td>
        <td><%= link_to 'Remove from me from group', course_path(group[0]),
            data: {:confirm => 'Are you sure?'}, :method => :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



<h4>THere are <%= @groups.length %> groups for this course!</h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if @groups.length == 0 %>
    	<tr> TABLE IS EMPTY  </tr>
    <% end %>
    <% @groups.each do |group| %>
      <tr>
      <td><%= group[1]["name"] %> </td>
        <td><%= link_to 'Show', course_path(group[0]) %></td>
        <td><%= link_to 'Add to My Course', new_course_path(group[0]) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<h3>Meetings INFO </h3>
<h4>Meetings you are currecntly a part of <%= @mymeetings.length %>!</h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if @mymeetings.length == 0 %>
    	<tr> TABLE IS EMPTY  </tr>
    <% end %>

    <% @mymeetings.each do |meeting| %>

      <tr>
      <td><%= meeting[1]["name"] %> </td>
        <td><%= link_to 'Show', course_path(meeting[0]) %></td>
        <td><%= link_to 'Remove from me from meeting', course_path(meeting[0]),
            data: {:confirm => 'Are you sure?'}, :method => :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



<h4>THere are <%= @meetings.length %> meetings for this course!</h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
    <% if @meetings.length == 0 %>
    	<tr> TABLE IS EMPTY  </tr>
    <% end %>
  <tbody>
    <% @meetings.each do |meeting| %>
      <tr>
      <td><%= meeting[1]["name"] %> </td>
        <td><%= link_to 'Show', course_path(meeting[0]) %></td>
        <td><%= link_to 'Add to My Course', new_course_path(meeting[0]) %></td>
      </tr>
    <% end %>
  </tbody>
</table>




<%= link_to 'Back', courses_path %>

